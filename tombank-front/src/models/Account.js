export default class Account {
  constructor(accid, accNumber, balance, transactions) {
    this.accid = accid;
    this.accNumber = accNumber;
    this.balance = balance;
    this.transactions = transactions;
  }
}
