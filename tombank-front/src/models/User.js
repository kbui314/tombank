export default class User {
  constructor(userid, username, password, firstName, lastName, phone) {
    this.userid = userid;
    this.username = username;
    this.password = password;
    this.firstName = firstName;
    this.lastName = lastName;
    this.phone = phone;
  }
}
